version: '3.8'

services:
  jira_app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: jira_app
    restart: unless-stopped
    ports:
      - "8081:8081"
    volumes:
      - ./shared_volume:/shared
    environment:
      - SECRET_KEY=${SECRET_KEY}
      - JIRA_INSTANCE=${JIRA_INSTANCE}
      - FLASK_DEBUG=${FLASK_DEBUG:-False}
      - FLASK_HOST=0.0.0.0
      - FLASK_PORT=8081
      - LOG_FILE=/shared/app.log
      - SESSION_FILE=/shared/session_data.json
      - UPDATED_ISSUES_LOG=/shared/updated_issues.log
    networks:
      - jira_network
    healthcheck:
      test: ["CMD", "python", "-c", "import requests; requests.get('http://localhost:8081')"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 40s

networks:
  jira_network:
    driver: bridge
